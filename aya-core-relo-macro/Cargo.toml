[package]
name = "aya-core-relo-macro"
version = "0.1.0"
edition = "2024"
description = "Proc macro for core_read!: emits CO-RE relocation markers into BPF ELF sections"

[workspace]

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
